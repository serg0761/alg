#В диапазоне натуральных чисел от 2 до 99 определить, сколько из них кратны
# любому из чисел в диапазоне от 2 до 9. 

numbers = [i for i in range(2, 100)]


for i in range(2, 10):
    k = 0
    for num in numbers:
        if num % i == 0:
            k += 1
        else:
            pass
    print('В диапазоне от 2 до 99 содержится', k, 'чисел кратных', i)
#    Можно и в новый массив полученные значения добавлять, но,
#   судя по задаче, такой необходимости нет... а вот 8 ответов - есть
